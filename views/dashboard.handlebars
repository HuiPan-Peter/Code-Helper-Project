{{!-- <section class="hero has-background-grey-lighter">
    <div class="hero-body">
        <div class="container">
            <div class="columns is-centered">
                <div class="column">
                    <form class="box">
                        <label class="label is-large" class="label">Create New
                            Blog Post</label>
                        <div class="field">
                            <label class="label">Subject / Title</label>
                            <div class="control">
                                <input
                                    type="text"
                                    placeholder="10 Ways Express.js Makes Creating REST API's Easier"
                                    class="input subject-input"
                                    required
                                />
                            </div>
                        </div>
                        <div class="field-body">
                            <div class="field">
                                <label class="label">Content</label>
                                <div class="control">
                                    <textarea
                                        class="textarea content-input"
                                        placeholder="Building an API is a rewarding way to allow data to flow between apps and create compelling functionality in your latest project without having to start from scratch..."
                                        required
                                    ></textarea>
                                </div>
                            </div>
                        </div>
                        <div class="field is-grouped is-grouped-right mt-3">
                            <button class="button is-success submit-post">
                                Submit
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</section>
<section class="hero has-background-grey-lighter hug">
    <div class="hero-body">
        <div class="container">
            <div class="columns is-centered">
                <div class="column">
                    <form class="box">
                        <table
                            class="table is-bordered is-striped is-narrow is-hoverable is-fullwidth"
                        >
                            <thead>
                                <tr>
                                    <th class="table-id">ID</th>
                                    <th class="table-title">Title</th>
                                    <th class="table-author">Author</th>
                                    <th class="table-created">Created</th>
                                    <th class="table-edit">Edit</th>
                                    <th class="table-delete">Delete</th>
                                </tr>
                            </thead>

                            <tbody>
                                {{#each posts}}
                                    <tr>
                                        <td>{{this.id}}</td>
                                        <td><a
                                                href="/post/{{this.id}}"
                                            >{{this.title}}</a></td>
                                        <td>{{this.user.username}}</td>
                                        <td>{{format_time this.createdAt}}</td>
                                        <td><a
                                                href="/edit/{{this.id}}"
                                            >Edit</a></td>
                                        <td><a
                                                class="delete-post"
                                                data-id="{{this.id}}"
                                            >Delete</a></td>
                                    </tr>
                                {{/each}}
                            </tbody>
                        </table>
                    </form>
                </div>
            </div>
        </div>
    </div>
</section>
<script src="/js/dashboard.js"></script> --}}


<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Code-Helper-Project</title>
  <link rel="stylesheet" href="/css/style.css">
</head>

<body class="bg-white text-black">
  <div class="flex flex-col h-screen justify-between">
    <header class="flex justify-between items-center bg-blue-600 text-white p-4 shadow-lg">
      <h1 class="text-2xl font-semibold">
        <a href="/" class="hover:underline">Code-Helper-Project</a>
      </h1>
      <nav>
        {{#if logged_in}}
        <a href="/dashboard" class="mr-4 bg-green-600 p-2 rounded shadow-md hover:bg-green-700 transition-all">Dashboard</a>
        <a href="/profile" class="mr-4 bg-green-600 p-2 rounded shadow-md hover:bg-green-700 transition-all">Profile</a>
        <button id="logout" class="bg-green-600 p-2 rounded shadow-md hover:bg-green-700 transition-all">Logout</button>
        {{else}}
        <a href="/login" class="mr-4 bg-green-600 p-2 rounded shadow-md hover:bg-green-700 transition-all">Login</a>
        <a href="/signup" class="bg-green-600 p-2 rounded shadow-md hover:bg-green-700 transition-all">Signup</a>
        {{/if}}
      </nav>
    </header>

    <main class="flex-grow container mx-auto mt-5 mb-5 p-5 bg-gray-100 rounded shadow-md">
      {{{ body }}}
    </main>

    <footer class="bg-blue-600 text-white text-center py-4 shadow-lg">
      <p>© 2023 Code-Helper-Project. All rights reserved.</p>
    </footer>
  </div>
</body>

</html>
